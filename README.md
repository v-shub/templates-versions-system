# Система контроля версий шаблонов

Групповой проект по разработке системы управления версиями шаблонов документов.

## Описание

Система контроля версий шаблонов — это веб-приложение для управления шаблонами документов с поддержкой версионирования, поиска и метаданных. Позволяет загружать, хранить, версионировать и искать шаблоны документов.

## Технологический стек

### Backend
- **Node.js** + **TypeScript**
- **Express.js** — веб-фреймворк
- **MongoDB** — база данных для хранения шаблонов
- **Elasticsearch** — полнотекстовый поиск
- **Redis** — кэширование
- **Swagger** — документация API

### Frontend
- **React** + **TypeScript**
- **Material-UI** — компоненты интерфейса
- **React Query** — управление состоянием и запросами
- **Axios** — HTTP-клиент

### Инфраструктура
- **Docker** + **Docker Compose** — контейнеризация и оркестрация

## Структура проекта

```
templates-versions-system/
├── backend/          # Backend приложение
│   ├── src/
│   │   ├── controllers/   # Контроллеры
│   │   ├── models/        # Модели данных
│   │   ├── routes/        # Маршруты API
│   │   ├── services/      # Бизнес-логика
│   │   └── swagger/       # Документация API
│   └── uploads/           # Загруженные файлы
├── frontend/         # Frontend приложение
│   └── src/
│       ├── components/    # React компоненты
│       └── services/      # API сервисы
└── docker-compose.yml     # Конфигурация Docker
```

## Быстрый старт

### Требования
- Docker и Docker Compose
- Node.js 18+ (для локальной разработки)

### Запуск с помощью Docker

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd templates-versions-system
```

2. Запустите все сервисы:
```bash
docker-compose up -d
```

3. Приложение будет доступно по адресам:
   - Frontend: http://localhost
   - Backend API: http://localhost:3000
   - API Документация (Swagger): http://localhost:3000/api-docs
   - MongoDB: localhost:27017
   - Elasticsearch: http://localhost:9200
   - Redis: localhost:6379

### Локальная разработка

#### Backend
```bash
cd backend
npm install
npm run dev
```

#### Frontend
```bash
cd frontend
npm install
npm start
```

## Основные возможности

- 📤 **Загрузка шаблонов** — загрузка файлов шаблонов с метаданными
- 📝 **Управление версиями** — автоматическое версионирование при обновлении
- 🔍 **Поиск** — полнотекстовый поиск по шаблонам с использованием Elasticsearch
- 📊 **Метаданные** — категории, теги, отделы, статусы (черновик/утвержден/устаревший)
- 📥 **Экспорт данных** — экспорт информации о шаблонах в различных форматах
- 🔄 **История версий** — просмотр истории изменений шаблонов

## API

API документация доступна через Swagger UI по адресу: `http://localhost:3000/api-docs`

Основные endpoints:
- `GET /api/templates` — список шаблонов
- `POST /api/templates` — создание нового шаблона
- `GET /api/templates/:id` — получение шаблона по ID
- `PUT /api/templates/:id` — обновление шаблона
- `DELETE /api/templates/:id` — удаление шаблона
- `GET /api/templates/search` — поиск шаблонов
- `GET /api/templates/:id/versions` — история версий

## Тестирование

### Backend тесты
```bash
cd backend
npm test              # Запуск всех тестов
npm run test:watch    # Тесты в режиме наблюдения
npm run test:coverage # Тесты с покрытием кода
```

## Лицензия

Этот проект создан в рамках группового проекта.

