# Система контроля версий шаблонов

Групповой проект по разработке системы управления версиями шаблонов документов.

## Описание

Система контроля версий шаблонов — это веб-приложение для управления шаблонами документов с поддержкой версионирования, поиска и метаданных. Позволяет загружать, хранить, версионировать и искать шаблоны документов.

## Технологический стек

### Backend
- **Node.js** + **TypeScript**
- **Express.js** — веб-фреймворк
- **MongoDB** — база данных для хранения шаблонов
- **Elasticsearch** — полнотекстовый поиск
- **Redis** — кэширование
- **Swagger** — документация API
- **AWS SDK** — работа с S3-совместимыми хранилищами
- **JSZip** — обработка архивов Office (DOCX, XLSX, PPTX)
- **pdf-parse** — извлечение текста из PDF
- **fast-xml-parser** — парсинг XML в документах Office
- **diff** — сравнение версий файлов

### Frontend
- **React** + **TypeScript**
- **Material-UI** — компоненты интерфейса
- **React Query** — управление состоянием и запросами
- **Axios** — HTTP-клиент
- **date-fns** — работа с датами и форматирование
- **xlsx** — экспорт данных в Excel
- **file-saver** — сохранение файлов на клиенте
- **@mui/x-date-pickers** — компоненты выбора даты

### Инфраструктура
- **Docker** + **Docker Compose** — контейнеризация и оркестрация

## Структура проекта

```
templates-versions-system/
├── backend/          # Backend приложение
│   ├── src/
│   │   ├── controllers/   # Контроллеры
│   │   ├── models/        # Модели данных
│   │   ├── routes/        # Маршруты API
│   │   ├── services/      # Бизнес-логика
│   │   └── swagger/       # Документация API
│   └── uploads/           # Загруженные файлы
├── frontend/         # Frontend приложение
│   └── src/
│       ├── components/    # React компоненты
│       └── services/      # API сервисы
└── docker-compose.yml     # Конфигурация Docker
```

## Быстрый старт

### Требования
- Docker и Docker Compose
- Node.js 18+ (для локальной разработки)

### Запуск с помощью Docker

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd templates-versions-system
```

2. Запустите все сервисы:
```bash
docker-compose up -d
```

3. Приложение будет доступно по адресам:
   - Frontend: http://localhost
   - Backend API: http://localhost:3000
   - API Документация (Swagger): http://localhost:3000/api-docs
   - MongoDB: localhost:27017
   - Elasticsearch: http://localhost:9200
   - Redis: localhost:6379

### Локальная разработка

#### Backend
```bash
cd backend
npm install
npm run dev
```

#### Frontend
```bash
cd frontend
npm install
npm start
```

## Конфигурация

### Хранилище файлов

Система поддерживает два типа хранилища:

#### Локальное хранилище (по умолчанию)
Файлы сохраняются в локальной директории `backend/uploads/`. Для использования локального хранилища установите:
```env
STORAGE_TYPE=local
UPLOAD_PATH=./uploads
BASE_URL=http://localhost:3000
```

#### Облачное хранилище (S3/Yandex Cloud)
Для использования облачного хранилища (например, Yandex Object Storage) установите:
```env
STORAGE_TYPE=s3
S3_BUCKET=your-bucket-name
S3_REGION=ru-central1
S3_ACCESS_KEY=your-access-key
S3_SECRET_KEY=your-secret-key
S3_ENDPOINT=https://storage.yandexcloud.net
```

Система автоматически определяет тип хранилища и работает прозрачно с обоими вариантами.

## Основные возможности

### Базовые функции
- 📤 **Загрузка шаблонов** — загрузка файлов шаблонов с метаданными
- 📝 **Управление версиями** — автоматическое версионирование при обновлении
- 🔍 **Поиск** — полнотекстовый поиск по шаблонам с использованием Elasticsearch
- 📊 **Метаданные** — категории, теги, отделы, статусы (черновик/утвержден/устаревший)
- 📥 **Экспорт данных** — экспорт информации о шаблонах в различных форматах
- 🔄 **История версий** — просмотр истории изменений шаблонов

### Новые возможности

#### 🔍 Расширенный поиск
- **Многофакторный поиск** — поиск с фильтрами по категории, отделу, статусу, тегам, автору
- **Поиск по датам** — фильтрация по диапазону дат создания/изменения
- **Поиск по размеру файла** — фильтрация по минимальному и максимальному размеру
- **Комбинированные фильтры** — одновременное использование нескольких критериев поиска

#### 🔄 Сравнение версий
- **Детальное сравнение версий** — визуальное сравнение двух версий шаблона
- **Diff-визуализация** — отображение изменений в текстовых файлах с подсветкой добавленных и удаленных строк
- **Сравнение метаданных** — автоматическое выявление изменений в метаданных между версиями
- **Сравнение содержимого файлов** — извлечение и сравнение текста из документов Office и PDF
- **Статистика изменений** — подсчет количества изменений в метаданных и содержимом

#### 📄 Предпросмотр файлов
- **Предпросмотр документов** — просмотр содержимого файлов без скачивания
- **Поддержка форматов** — предпросмотр PDF, изображений, текстовых файлов
- **Масштабирование** — увеличение и уменьшение масштаба просмотра
- **Полноэкранный режим** — просмотр в полноэкранном режиме
- **Прямое скачивание** — возможность скачать файл прямо из окна предпросмотра

#### ☁️ Облачное хранилище
- **Поддержка S3-совместимых хранилищ** — интеграция с облачными хранилищами
- **Yandex Object Storage** — нативная поддержка Yandex Cloud Object Storage
- **Гибкая конфигурация** — переключение между локальным и облачным хранилищем
- **Автоматическая миграция** — прозрачная работа с файлами независимо от типа хранилища

#### 📑 Обработка документов Office
- **Извлечение текста из DOCX** — автоматическое извлечение текста из документов Word
- **Извлечение текста из XLSX** — извлечение данных из таблиц Excel
- **Извлечение текста из PPTX** — извлечение текста из презентаций PowerPoint
- **Индексация содержимого** — автоматическая индексация извлеченного текста для поиска

#### 📄 Обработка PDF
- **Извлечение текста из PDF** — автоматическое извлечение текстового содержимого
- **Индексация PDF** — полнотекстовый поиск по содержимому PDF-документов
- **Сравнение PDF** — сравнение текстового содержимого PDF между версиями

#### 📊 Расширенный экспорт данных
- **Экспорт в Excel** — экспорт данных о шаблонах в формате XLSX с форматированием
- **Экспорт в CSV** — экспорт в CSV для работы с данными в других приложениях
- **Экспорт в PDF** — генерация PDF-отчетов с информацией о шаблонах
- **Выборочный экспорт** — выбор полей для экспорта (название, описание, метаданные и т.д.)
- **Экспорт по фильтрам** — экспорт только отфильтрованных данных
- **Экспорт выбранных** — экспорт только выбранных шаблонов

#### 🎯 Дополнительные функции
- **Кэширование** — использование Redis для кэширования результатов поиска и сравнения версий
- **Контроль целостности** — вычисление и проверка контрольных сумм файлов (checksum)
- **Поддержка множественных форматов** — работа с TXT, DOCX, XLSX, PPTX, PDF, XML и другими форматами

## API

API документация доступна через Swagger UI по адресу: `http://localhost:3000/api-docs`

### Основные endpoints

#### CRUD операции
- `GET /api/templates` — список шаблонов с пагинацией и фильтрацией
- `POST /api/templates` — создание нового шаблона (с загрузкой файла)
- `GET /api/templates/:id` — получение шаблона по ID
- `PUT /api/templates/:id` — обновление шаблона (с возможностью загрузки нового файла)
- `DELETE /api/templates/:id` — удаление шаблона

#### Поиск
- `GET /api/templates/search` — базовый полнотекстовый поиск
- `GET /api/templates/search/enhanced` — расширенный поиск с множественными фильтрами
- `GET /api/templates/autocomplete` — автодополнение для поисковых запросов

#### Файлы
- `GET /api/templates/:id/download` — скачивание файла шаблона
- `GET /api/templates/:id/preview` — предпросмотр файла шаблона

#### Версии
- `GET /api/templates/:id/versions` — получение истории версий шаблона
- `GET /api/templates/:id/versions/compare/:version1Id/:version2Id` — сравнение двух версий
- `POST /api/templates/:id/versions` — загрузка новой версии шаблона
- `POST /api/templates/:id/versions/:versionId/restore` — восстановление версии

#### Метаданные и статистика
- `GET /api/templates/:id/metadata` — получение метаданных шаблона
- `PATCH /api/templates/:id/status` — обновление статуса шаблона
- `GET /api/templates/stats` — статистика по шаблонам

#### Справочники
- `GET /api/categories` — список категорий
- `GET /api/departments` — список отделов
- `GET /api/tags` — популярные теги

## Тестирование

### Backend тесты
```bash
cd backend
npm test              # Запуск всех тестов
npm run test:watch    # Тесты в режиме наблюдения
npm run test:coverage # Тесты с покрытием кода
```

## Лицензия

Этот проект создан в рамках группового проекта.

